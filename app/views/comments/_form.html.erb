<%= form_with model: [commentable, Comment.new] do |form| %>
  <div class="field">
    <%= form.label :comment %>
    <%= form.text_area :comment, required: :required %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
